/**
 *
 * {{ properCase name }} Model
 *
 */

const db = require("../../dbConnection");

const {{ camelCase name }} = {
    getAll{{ properCase name }}s:(callback) => {

        return db.query("Select * from {{ camelCase name }}",callback);

    },

    get{{ properCase name }}ById:(id,callback) => {

        return db.query("select * from {{ camelCase name }} where {{ camelCase name }}_id=?",[id],callback);
    }
};

module.exports = {{ camelCase name }};