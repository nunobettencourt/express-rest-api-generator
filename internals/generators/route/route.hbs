/**
 *
 * {{ properCase name }} Routes
 *
 */

const express = require('express');
const router = express.Router();
const {{ camelCase name }} = require('../models/{{ camelCase name }}Model');

router.get('/:id?',(req,res,next) => {

    if(req.params.id){

        {{ camelCase name }}.get{{ properCase name }}ById(req.params.id,function(err,rows){

            if(err)
            {
                res.json(err);
            }
            else{
                res.json(rows);
            }
        });
    }
    else{

        {{ camelCase name }}.getAll{{ properCase name }}s((err,rows) => {

            if(err)
            {
                res.json(err);
            }
            else
            {
                res.json(rows);
            }

        });
    }
});

module.exports=router;