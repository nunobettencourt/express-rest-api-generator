/**
 *
 * {{ properCase name }} Routes
 *
 */

var express = require('express');
var router = express.Router();
var {{ camelCase name }} = require('../models/{{ camelCase name }}Model');

router.get('/:id?',function(req,res,next){

    if(req.params.id){

        {{ camelCase name }}.get{{ properCase name }}ById(req.params.id,function(err,rows){

            if(err)
            {
                res.json(err);
            }
            else{
                res.json(rows);
            }
        });
    }
    else{

        {{ camelCase name }}.getAll{{ properCase name }}s(function(err,rows){

            if(err)
            {
                res.json(err);
            }
            else
            {
                res.json(rows);
            }

        });
    }
});

module.exports=router;